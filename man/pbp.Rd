% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{pbp}
\alias{pbp}
\title{CDF of the Beta Power Distribution}
\usage{
pbp(q, gamma, delta, lambda, lower_tail = TRUE, log_p = FALSE)
}
\arguments{
\item{q}{Vector of quantiles in (0,1).}

\item{gamma, }{delta, lambda Distribution parameters.}

\item{lower_tail}{Logical; if TRUE, returns F(q), else 1-F(q).}

\item{log_p}{Logical; if TRUE, returns log-probabilities.}
}
\description{
Computes F(q)= P(X ≤ q) for BP(γ, δ, λ).
}
\details{
\deqn{
  F(x)= I_{x^\lambda}(\gamma, \delta+1) \;=\; \mathrm{pbeta}(x^\lambda,\;\gamma,\;\delta+1).
}
The incomplete Beta approach is used for stable evaluation.
}
